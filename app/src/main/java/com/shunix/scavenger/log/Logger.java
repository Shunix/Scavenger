package com.shunix.scavenger.log;

/**
 * Every custom logger must implements this interface, custom class must have a default constructor without params
 * Created by shunix on 15-2-1.
 */
public interface Logger {
    public void log();
}
